# Search movements

here we have highlighted rows 1 and 4-5

```js showLineNumbers {1,4-5}
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

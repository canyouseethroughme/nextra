# View or Edit movement


Click [here](/some-random-path-not-existing) to read more.

here we have highlighted the word Counter

```js showLineNumbers /Counter/
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```
 
